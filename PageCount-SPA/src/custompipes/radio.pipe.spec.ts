/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RadioPipe } from './radio.pipe';

describe('Pipe: Radio', () => {
  it('create an instance', () => {
    let pipe = new RadioPipe();
    expect(pipe).toBeTruthy();
  });
});
